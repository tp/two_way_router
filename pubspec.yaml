name: two_way_router
description: A router converting URLs to objects and objects to URLs.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  meta: any

dev_dependencies:
  test: any
